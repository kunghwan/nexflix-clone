import { style } from "@vanilla-extract/css";

const styles = {
  // 문자열
  container: style({
    border: "1px solid",
  }),
  title: style({}),
  desc: style({}),
  icon: style({}),
};

export default styles;
